# pizza-api-backend-spring
How this maps to the current app Today the React form POSTs to Express at http://localhost:9009/api/order (Form.js). Next step is Spring MVC controllers/entities that match that payload (fullName, size, toppings), then point the frontend at http://localhost:8080.  The Spring process is still running in the background. 
